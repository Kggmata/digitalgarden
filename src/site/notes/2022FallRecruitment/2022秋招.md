```dataviewjs
const term = "#job/2022FallRecuirtment/简历筛选"
const content = await dv.io.load(dv.current().file.path)
let index=0;
const lines = content.split("\n").filter(line => line.contains(term) && !line.contains("const term = ") && line!="* 简历筛选").map(item=>item.replace("*",""))
let total=lines.length
dv.paragraph(lines)
dv.paragraph("\n### total number "+total)
```